<?php 
/*
			 const divRow  = document.createElement("div");
			 const divCol = document.createElement("div");
			//idDesglose.removeChild(idDesglose.firstChild);
			//alert(idDesglose.childNodes.length);
			if (idDesglose.childNodes.length != 0) {
				idDesglose.removeChild(idDesglose.firstChild);
			}	
			const tableDesglose = document.createElement("table");
			
			const tbodyDesglose = document.createElement("tbody");

			//HEAD TABLE
					const theadDesglose = document.createElement("thead");
					const filaHeadDesglose = document.createElement("tr");
					var datosHeadDeslgose = ["NUM. PAGO","FECHA","BALANCE","CUATA", "DEMORA", "TOTAL", "MONTO"];
					for (var i = 0; i < datosHeadDeslgose.length; i++) {
						var celdaHeadDesglose = document.createElement("th");
						var textCeldaHeadDesglose = document.createTextNode(datosHeadDeslgose[i]);
						celdaHeadDesglose.appendChild(textCeldaHeadDesglose);
						filaHeadDesglose.appendChild(celdaHeadDesglose);
					}
					//theadDesglose.appendChild(filaHeadDesglose);
					tableDesglose.className += "table table-hover";
					tableDesglose.appendChild(filaHeadDesglose);
					divCol.className += "col-lg-12";
					divCol.appendChild(tableDesglose);
					divRow.className += "row";
					divRow.appendChild(divCol);

					idDesglose.appendChild(divRow);

					//END HEAD TABLE
			idDesglose.style.display = (idDesglose.style.display == 'none') ? 'block' : 'none'; //damos un atributo display:none que oculta el div
		*/



 ?>